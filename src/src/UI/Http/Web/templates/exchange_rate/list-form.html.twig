{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <div class="col-md-12 text-center">
        <div class="jumbotron">
            <form name="getExchangeListTable" action="{{ path('getExchangeListTable') }}" method="post">
                <div class="form-row d-flex justify-content-center">
                    <div class="col-md-3 mb-3">
                        <label for="exchange-rate-date">Data kursu</label>
                        <input type="text" class="form-control datepicker" id="exchange-rate-date" name="exchange-rate-date">
                    </div>
                </div>
                <button class="btn btn-primary" type="submit">Pokaż</button>
            </form>
        </div>
    </div>
</div>

{% include 'errors.html.twig' %}

{% endblock %}
